{
  "name": "chat-app-mern",
  "version": "1.0.0",
  "description": "This project is a Chatting Application",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "server": "node server.js",
    "build": "npm install && cd client && npm install && npm run build",
    "client": "npm start --prefix client",
    "server-install": "npm install",
    "clientinstall": "npm install --prefix client",
    "dev": "concurrently \"npm run server\" \"npm run client\"",
    "heroku-postbuild": "cd client && npm install --force && npm run build",
    "render-postbuild": "NPM_CONFIG_PRODUCTION=false npm install --force --prefix client && npm run build --prefix client"
  },
  "author": "Aman Poddar",
  "license": "ISC",
  "dependencies": {
    "bcryptjs": "^2.4.3",
    "concurrently": "^8.2.2",
    "cors": "^2.8.5",
    "dotenv": "^16.3.1",
    "express": "^4.18.2",
    "express-async-handler": "^1.2.0",
    "jsonwebtoken": "^9.0.1",
    "mongoose": "^7.3.2",
    "nodemon": "^3.0.1",
    "socket.io": "^4.7.5"
  }
}
